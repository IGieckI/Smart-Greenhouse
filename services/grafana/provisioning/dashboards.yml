apiVersion: 1
providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    # Aggiungi questa riga per sbloccare il salvataggio dalla UI
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/json